<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 边框 -->
    <stroke
        android:width="0dp"
        android:color="@color/measure_test_btn_bg" />
    <solid android:color="@color/measure_test_btn_bg" />
    <!-- 圆角 -->
    <corners android:radius="5dp"/>
    <!-- 边距 -->
    <padding
        android:top="0dp"
        android:bottom="0dp"
        android:left="6dp"
        android:right="6dp"/>

</shape>